title: Algorithm_learning
description: 'Record algorithm learning experience, store some codes and templates.'
template:
  repo: algorithm_learning-template
  name: algorithm_learning
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
